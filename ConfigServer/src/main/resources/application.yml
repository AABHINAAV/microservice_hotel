server:
  port: 8900

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/AABHINAAV/microservice_hotel_configuration
          clone-on-start: true